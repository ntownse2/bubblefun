var red = [0,100, 63];
var purple = [280, 50, 60];
var myName = "Tess";
var letterColors = [red, purple];

if (myName) {
    bubbleShape = "circle";
    } else{
      bubbleShape = "circle";
      }:

drawName (myName, letterColors);
bounceBubbles();
